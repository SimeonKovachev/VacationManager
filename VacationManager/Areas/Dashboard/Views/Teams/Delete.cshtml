@model VacationManager.Entity.Team

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Dashboard/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm border-0 rounded-lg mt-3">
                <div class="card-header"><h3 class="text-center font-weight-light my-4">Delete Project</h3></div>
                <div class="card-body mx-5">
                    <h5 class="alert alert-danger">
                        Are you sure you want to delete this?
                    </h5>
                    <div>

                        <hr />
                        <dl class="row">
                            <div class="col-sm-3">
                                <dt class="text-primary">Team Name</dt>
                                <dd>@Html.DisplayFor(model => model.Name)</dd>
                            </div>
                            <div class="col-sm-3">
                                <dt class="text-primary">Number of Workers</dt>
                                <dd>@Html.DisplayFor(model => model.NumOfWorkers)</dd>
                            </div>          
                        </dl>
                    </div>
                    <div class="btn-group" role="group">
                        <a href="@Url.Action("Index", "Teams")" class="btn btn-outline-primary"><i class="fa-solid fa-chevron-left"></i> Back to List</a>
                        <button type="submit" class="btn btn-danger"> <i class="fa-solid fa-trash-can"></i></button>
                    </div>
                </div>
            </div>
        </div>


    </div>
}